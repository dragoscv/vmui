node_modules
.next
out
dist
.env
.env.local

# Local-only credentials for the KVM guests. NEVER commit this directory —
# see .private/README.md and scripts/lib/guest-credentials.sh
.private/

# toolkit build artifacts (the .dmg is produced by install.sh --dmg)
toolkit/*.dmg
toolkit/build/

*.log
.DS_Store
vmui.db
vmui.db-*
drizzle/meta
.vscode/*
!.vscode/extensions.json
.pnpm-cache/

# Go CLI build artifacts
vmui-cli
vmui-cli.exe

# Playwright artifacts
test-results/
playwright-report/
playwright/.cache/

__pycache__/

# Microsoft fonts copied for the Pi renderer; licensed, never commit
turzx/fonts/

# Agent scratch. Also keeps Tailwind v4's source scan out of it: a literal
# `rounded-[var(--radius-sm|md|lg|xl)]` in a brief there breaks globals.css.
.copilot-tmp/
