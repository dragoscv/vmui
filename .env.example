# 32-byte hex master key for AES-256-GCM credential encryption.
# Generate with:  pnpm keygen
VMUI_MASTER_KEY=

# Optional: override database path (defaults to ./vmui.db at the project root)
# VMUI_DB_PATH=./vmui.db
